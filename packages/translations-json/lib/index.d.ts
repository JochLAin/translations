import { MacroParams } from "babel-plugin-macros";
import { LoaderType } from "./types";
declare const _default: (loader: LoaderType) => ({ babel, config, references }: MacroParams) => void;
export default _default;
